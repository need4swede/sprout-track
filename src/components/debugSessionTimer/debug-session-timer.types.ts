/**
 * Types for the DebugSessionTimer component
 */

/**
 * Position interface for tracking the timer's position on screen
 */
export interface Position {
  x: number;
  y: number;
}

/**
 * Props for the DebugSessionTimer component
 * Currently empty as the component doesn't accept any props,
 * but defined for future extensibility
 */
export interface DebugSessionTimerProps {}
